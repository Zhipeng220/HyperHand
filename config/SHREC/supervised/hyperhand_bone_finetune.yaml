# 配置文件: config/SHREC/supervised/hyperhand_bone_finetune.yaml

# 1. 工作目录
work_dir: ./work_dir/SHREC/hyperhand_bone_adamw_finetune

# 2. 核心：加载 Bone 流 Epoch 65 权重
weights: ./work_dir/SHREC/hyperhand_bone_adamw/epoch065_acc57.62_model.pt

# 3. 训练进度 (65 -> 125)
start_epoch: 65
num_epoch: 125

# 4. 优化器 (保持稳健)
optimizer: AdamW
base_lr: 0.0001
optimizer_args:
  weight_decay: 0.1

step: []

# 5. 模型定义
model: net.ctrgcn.Model
model_args:
  in_channels: 3
  num_class: 14
  num_point: 22
  num_person: 1
  drop_out: 0.6
  graph: 'graph.shrec.Graph'
  graph_args:
    labeling_mode: 'spatial'
  num_hyperedges: 16
  adaptive: True

# 6. 数据加载 (🔴 之前漏了 test_feeder，这里补上了)
train_feeder: feeder.feeder_egogesture.Feeder
test_feeder: feeder.feeder_egogesture.Feeder  # <--- 补上这一行！

train_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_label.pkl
  split: train
  bone: True        # Bone 模式
  window_size: 50
  shear_amplitude: 0.5
  temperal_padding_ratio: 6
  random_rot: True
  random_choose: True
  random_move: True
  random_shift: True
  normalization: True # 必须为 True
  debug: False
  use_mmap: True

test_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_label.pkl
  split: test
  bone: True        # Bone 模式
  window_size: 50
  normalization: True # 必须为 True
  debug: False
  use_mmap: True

batch_size: 32
test_batch_size: 32
num_worker: 4
device: [0]
use_gpu: True
log_interval: 100
save_interval: 1
eval_interval: 1
save_result: True
show_topk: [1, 5]
grad_clip_norm: 1.0