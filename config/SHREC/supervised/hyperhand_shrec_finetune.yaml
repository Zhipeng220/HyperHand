# 1. 工作目录 (新建一个，避免混淆)
work_dir: ./work_dir/SHREC/hyperhand_joint_adamw_finetune

# 2. 核心：加载刚才跑出来的 Best Model (Epoch 65, 85.12%)
# 请确保路径和文件名完全一致
weights: ./work_dir/SHREC/hyperhand_joint_adamw/epoch065_acc85.12_model.pt

# 3. 训练进度控制
start_epoch: 65
num_epoch: 90       # 再跑 25 个 Epoch (65 -> 90)

# 4. 优化器调整 (微调模式)
optimizer: AdamW
base_lr: 0.00005   # 🔴 [关键] 降低 10 倍！从 1e-4 降到 1e-5
optimizer_args:
  weight_decay: 0.05
  # eps: 1e-4       # 代码里已经强制写死 eps=1e-4 了，这里配不配都行

# 5. 禁用 Step (我们手动降了 LR，不需要自动衰减了)
step: []

# 6. 其他参数 (保持不变)
model: net.ctrgcn.Model
model_args:
  in_channels: 3
  num_class: 14
  num_point: 22
  num_person: 1
  drop_out: 0.5
  graph: 'graph.shrec.Graph'
  graph_args:
    labeling_mode: 'spatial'
  num_hyperedges: 16
  adaptive: True

train_feeder: feeder.feeder_egogesture.Feeder
train_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/train_label.pkl
  split: train
  bone: False
  window_size: 50
  shear_amplitude: 0.5
  temperal_padding_ratio: 6
  random_rot: True
  random_choose: True
  random_move: True
  random_shift: True
  normalization: True
  debug: False
  use_mmap: True

test_feeder: feeder.feeder_egogesture.Feeder
test_feeder_args:
  data_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_data.npy
  label_path: /Users/gzp/Desktop/exp/DATA/SHREC2017_data/val_label.pkl
  split: test
  bone: False
  window_size: 50
  normalization: True
  debug: False
  use_mmap: True

batch_size: 32
test_batch_size: 32
num_worker: 4
device: [0]
use_gpu: True
log_interval: 100
save_interval: 1
eval_interval: 1
save_result: True
show_topk: [1, 5]
grad_clip_norm: 1.0